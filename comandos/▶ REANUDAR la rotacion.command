#!/bin/bash
# Doble-click: reanuda la rotacion normal (un video distinto en cada bloqueo).
cd "$(dirname "$0")"
rm -f "PAUSA"
echo ""
echo "  ▶  ROTACION REANUDADA"
echo ""
echo "  Vuelve a cambiar de video en cada bloqueo de pantalla."
echo ""
echo "  (Podes cerrar esta ventana)"
